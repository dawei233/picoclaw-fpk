#!/bin/bash
# 配置修改前钩子
APP_DEST="${TRIM_APPDEST:-}"
if [ -n "${APP_DEST}" ] && [ -x "${APP_DEST}/cmd/main" ]; then
  "${APP_DEST}/cmd/main" stop 2>/dev/null
fi
echo "[picoclaw] config_init: ok"
exit 0
