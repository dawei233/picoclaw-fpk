#!/bin/bash
# 安装完成钩子：fnOS 在解压并写库后调用
# 这里只保证 binary +x（极少见 fnpack 解包会丢位），其他 mkdir/cp 全交给 fnOS
APP_DEST="${TRIM_APPDEST:-}"
if [ -n "${APP_DEST}" ] && [ -d "${APP_DEST}/bin" ]; then
  chmod +x "${APP_DEST}/bin/"* 2>/dev/null
fi
echo "[picoclaw] install_callback: ok"
exit 0
