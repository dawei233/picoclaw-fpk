#!/bin/bash
# 升级完成钩子：fnOS 在覆盖文件后调用
APP_DEST="${TRIM_APPDEST:-}"
if [ -n "${APP_DEST}" ] && [ -d "${APP_DEST}/bin" ]; then
  chmod +x "${APP_DEST}/bin/"* 2>/dev/null
fi
echo "[picoclaw] upgrade_callback: ok (用户配置与数据由 fnOS 托管保留，重启应用即可加载新版)"
exit 0
